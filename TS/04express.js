/* var price, total, taxrate : number;
price = 1200;   // （1） 식: 변수=리터널;
taxrate = 0.08; // （2）
total = price * (1 + taxrate);  // （3） 식: 변수 = 변수 * (리터널+변수)
alert(total); */
/* class Monster{  // 사용자정의 데이터타입
    name:string;  // 名前
    hp:number;    // 生命力
}
var aMonster: Monster = new Monster();
aMonster.name = "スライム";     // （1）
aMonster.hp = 10;               // （2）
//alert(aMonster.name + "(" + aMonster.hp + ")が現れた!");    // （3）
alert(`${aMonster.name}(${aMonster.hp})が現れた!`);
 */
/* var x, y: number;
x = 10;
y = x / 0;
alert(y); */
/*
var count;
count = 0;
alert(count++);
alert(count); */
var a = 10, b = 20;
var c;
c = a + 1, b, 30; // （1）
alert(c);
